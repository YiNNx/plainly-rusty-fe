import React from 'react';
import Header from '../components/Header';
import BlogList from '../components/BlogList';

const Home: React.FC = () => {
    return (
        <div>
            <BlogList />
        </div>
    );
};

export default Home;
